matrix:
  include:
    - language: java
      jdk: oraclejdk11
      before_install:
        - cd backend
      install:
        - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
      script:
        - mvn clean install -B
